import Tree "../tree";
import Iter "mo:base/Iter";

module {
    // let nodeToInsert : Tree.Node = {
    //     base = 2 * 4;
    //     boundary = {
    //         value = 0;
    //         direction = #plus;
    //     };
    //     identifier = [1];
    //     value = "h";
    //     children = HashMap.HashMap<Nat, Tree.Node>(10, Nat.equal, Nat32.fromNat);
    // };

    func getIdentifierAfter(rootNode : Tree.Node, identifier : [Nat], base : Nat) : [Nat] {
        // The steps to get an identifier after a given identifier are as follows:
        // 1. If the identifier is empty, trap.

        // Strategy 1
        let newIdentifier = [];
    };

};
