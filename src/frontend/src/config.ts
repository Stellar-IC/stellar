import { canisterId as internetIdentityCanisterId } from '../../declarations/internet_identity';

export const INTERNET_IDENTITY_HOST = 'http://127.0.0.1:4943/';

export const canisters = {
  INTERNET_IDENTITY: {
    id: internetIdentityCanisterId,
  },
};
